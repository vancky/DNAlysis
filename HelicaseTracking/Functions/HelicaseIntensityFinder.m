function [ output ] = HelicaseIntensityFinder( config, spotFinder , helicaseImage )
    % Helicase Intensity Finder - Finds the intensity of helicases found by
    % the SpotFinder.
    
    L = length( spotFinder.circle );
    
    for i= 1:L
        spotFinder.circle(i).centers
    
    end
    
    


end

