function [ output ] = BeamshapeCorrection( config, image )
    % Beamshape Correction - Corrects for the beamshape profile
    % Analyses images with only the beamshape profile, generates a
    % normalized matrix with intensity values. Then uses this matrix to
    % correct the intensity of the other images.
    
        
    generateBeamshape=GenerateBeamshape(config)
    
    %for i=1:numimages
    %   output(:,:,i)=image.*beamshapeNormalization;
    %end

end

